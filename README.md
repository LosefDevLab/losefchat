# LosefChat | 一款简易的 DotNet 本地聊天模块

主要开发：阿龙

Copyright (C) 2025 LosefDevLab<br>
Copyright (C) 2018-Now XYLCS/XIT<br>
Copyright (C) 2019-2025 kakako Chat Community Studio(KCCS)<br>
Copyright (C) 2024-2025 PPPO Technological Team(PTT)<br>

以上合并称为 LosefChat开发团队<br>

Losefchat (2025) by LosefChat开发团队 2025-Now freedom-create in XFCSTD.<br>

XFCSTD PATH:/XFCSTD.license

> **注意**：本项目需要自行编译，请参考文末的编译指南。<br>
>
> 提示:  从第113个Commit开始,  本项目的所有代码所用许可证为GPL许可证

## 主要内容

- 1️⃣模块化
- 🧩支持MOD
- 🛜IPV4/IPV6
- 🛡️可TLS
- #️⃣命令行
- 😊占用低
- 🔐密码功能
- 🔏Pefender密码防破解
- 📱服务端管理
- ⬆️开放, 自由

## 部署极其方便

- **启动服务器**
  输入 `2`，然后输入端口号即可启动服务器。
- **内网穿透**
  如果您的电脑本身处于公网环境，则无需配置内网穿透。
- **命令控制**
  简单输入一小段文本即可对服务器进行操作。
- **连接客户端**
  输入 `1`，然后输入 IP 和端口即可连接服务器，开始聊天。
- **友好的下线方式**
  输入 `exit`，系统会自动发送“我下线了拜拜”的消息。
  （不过我们猜大多数人还是会直接关闭控制台窗口。）

## 模组支持

LosefChat 提供了强大的模组支持功能：

1. 拉取 LosefChat 的代码。
2. 将模组代码复制到模组开发区域。
3. 在模组运行区域添加安装代码。
4. 重新编译 LosefChat，即可使用第三方功能。

---

## 停止对 XSC 的开发

我们决定停止对 XShChat (XSC) 的开发，并从头开始构建一个全新的聊天模块。原因如下：

- 版本管理混乱，功能不够完善。
- 长期未更新（鸽了好几个月）。
- 代码全在内部了，连推远程都不带推的，咋的凭关系获得代码？我*我妈！
- 用户体验不佳，许多服主选择开发第三方服务端或客户端。

尽管目前仍有许多服务器（如 `kkko`、`pppo`）在使用 XSC，并对其充满美好回忆，但我们不得不做出这一艰难决定。

在此，我们向您保证，新项目 **LosefChat** 将致力于改进这些问题，并提供更好的用户体验。

## LosefChat 与 XShChat 的对比

XSC 用户体验和开发存在诸多问题。以下是两者的详细对比：

### 主要问题

1. **长期不更新**
   许多服主不得不自行开发新功能。
2. **反人类设计**
   测试服务器时，客户端和服务器是分开的。
3. **编译困难**
   需要手动创建 `.NET` 项目并拉取代码。
4. **缺乏关键功能**
   白名单和密码功能？那不远古笑话吗？密码更是笑话中的笑话。
5. 不还能用啊?
   Kote!他模仿Jia Tan啊!!!XSC代码里面被他整了收集你账号密码甚至验证码的数据收集代码,  还好，那些代码没被开源。这还能用啊？你可别忘了kkko泄露了超3000万的原文日志数据泄露！！！！

### 我们的改进

- **合并服务器和客户端**：无需分别启动，部署更加便捷。
- **优化文件系统**：修复了 XSC 中懒得解决的问题。
- **增强用户体验**：重新设计回复文本，使其更加友好。
- **新增功能**：
  - 支持 IPv4 和 IPv6 协议。
  - 提供详细日志记录和搜索功能。
  - 引入 MOD AREA，支持模组开发。
  - 因为我们也有惨痛的经历，加上因为没有密码功能，间接使我们工作室的一位成员...哎，添加这个密码功能也算是了了他的心愿吧。

### 功能对比表


| 特性           | LosefChat                              | XShChat                                                                          |
| -------------- | -------------------------------------- | -------------------------------------------------------------------------------- |
| **用户界面**   | 命令行界面                             | 命令行界面                                                                       |
| **用户管理**   | 支持封禁、踢出、解封用户，管理用户行为 | 同上                                                                             |
| **日志记录**   | 提供详细的日志记录功能，并支持日志搜索 | 功能相同                                                                         |
| **协议支持**   | 支持 IPv4 和 IPv6 协议                 | 仅支持 IPv4                                                                      |
| **跨平台支持** | 支持 Windows、Linux、macOS             | 同样支持，但是有一些缺陷                                                         |
| **开发维护**   | 模块化设计，便于调试                   | 类似设计，但存在一些问题                                                         |
| **可扩展性**   | 新增 MOD AREA，支持模组开发            | 相关计划已废弃                                                                   |
| **用户体验**   | 多处优化，操作更加简便                 | 用户普遍选择第三方工具                                                           |
| **编译体验**   | 下载后直接编译即可                     | 需要手动创建项目并拉取代码                                                       |
| **高危程度**   | 移除了有害和隐私数据收集代码           | kote模仿JiaTan,暴力收集设备隐私数据,凭一己之力泄露两个服务器过千万字原文日志数据 |
| **社区生态**   | 更多社区创作者参与开发                 | 生态相对薄弱                                                                     |
| **官方声誉**   | 良好的开发态度和社区支持               | 经典笑话：“我们应该用 XSC 原版”                                                |
| **密码功能**   | 新增密码功能，广受好评                 | 把这个远古笑话搬上来，你和说C++网络库将要发布的一起站边去                        |
| **安全通讯**   | TLS1.2安全通讯，防止隐私泄露           | 已经哭晕在厕所                                                                   |

---

## 如何编译？

请确保已安装 `dotnet` （版本 >= 8.0）、`openssl`（版本 >= 3.0）和 `git`，然后按照以下步骤操作：

```bash
git clone https://github.com/LosefDevLab/losefchat.git
cd losefchat
dotnet build
cd bin
cd Debug
cd net8.0
# 以下内容，仅需要安全通信服务端需要操作
openssl genpkey -algorithm RSA -out sfc.key -aes256
openssl req -new -key sfc.key -out sfc.csr
openssl x509 -req -days 365 -in sfc.csr -signkey sfc.key -out sfc.crt
# 此处仅为演示, 实际建议使用可靠的签名, 否则早晚被破解
# 微软40块钱一份的签名它不香吗？
# 以上内容仅服务端需要操作, 客户端连接到这种开启安全通讯的服务器需要在程序同目录下提供服务器开放的安全通讯证书
openssl pkcs12 -export -out sfc.pfx -inkey sfc.key -in sfc.crt
./losefchat
```

---

## 贡献&Git规范标准

CMTMSG规范

- 需要先创建当前要做的内容的计划issues, 这个issues可以是需要修复的、更新计划、功能添加
- 当做完这些内容/做了新内容的其中一部分/修改新内容的部分/修复一些bug/合并, 按情况分别提交cmtmsg:
  - Add for #x : xxxxxxx
  - Add part for #x : xxxxxxx
  - Update for #x : xxxxxxx
  - Fix in #x : xxxxxxx
  - Merge branch xx(branchname) to xx(branch) in #x : xxxxxx

Release & tag信息规范

- 请使用WVPB版本号规则
- tag无需标注
- Release标题为版本号
- Release描述需用MD格式
- Release描述需按照以下格式进行编写:

  ```
  本次更新:
  -----
  - (本次阶段的所有CMTMSG)
  ```
